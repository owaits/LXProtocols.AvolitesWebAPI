<button class="@CssClass btn @(IsSelected() ? "btn-primary active" : "btn-outline-primary")" style="@BackgroundImageCSS() @BorderColourCSS()" @onclick="@(async ()=> await Select())">@DisplayLegend()</button>