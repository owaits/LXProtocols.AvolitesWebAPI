<button class="@CssClass btn @(IsActive() ? "btn-primary active" : "btn-outline-primary")" style="@BackgroundImageCSS() @BorderColourCSS()" @onclick="@(async ()=> await Fire())">@DisplayLegend()</button>